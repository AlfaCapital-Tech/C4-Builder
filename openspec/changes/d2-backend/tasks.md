# Tasks: d2-backend

## 1. Зависимость D2

- [ ] 1.1 Добавить `@terrastruct/d2` в `optionalDependencies`; зафиксировать версию
- [ ] 1.2 Ленивый `require` движка только при наличии `.d2` в дереве; при отсутствии
      пакета — понятная ошибка с подсказкой установки

## 2. Абстракция рендереров по расширению

- [ ] 2.1 Обобщить сбор дерева: вместо `item.pumlFiles` — `item.diagrams[]`
      `{ file, ext, content, engine }`, `engine` = `plantuml` | `d2`
- [ ] 2.2 Диспетчеризовать `generateImages` по `engine`; сохранить путь PlantUML
      (java-direct + Smetana) без изменений, включая ditaa
- [ ] 2.3 Расширить regex поиска диаграмм в markdown до `\.(puml|d2)`; ссылки/имена
      выходов работают для обоих

## 3. D2-рендерер

- [ ] 3.1 Парсер графа импортов `.d2` (`@`/`...@`): сбор `{ путь: контент }` для
      виртуальной `fs` + материал для чексуммы кэша (аналог `foldIncludes`)
- [ ] 3.2 Рендер через `compile({ fs, inputPath, options })` → `render` → SVG;
      единый инстанс на прогон (warm-up один раз), конфиг `d2Layout` (dagre|elk)
- [ ] 3.3 Явный teardown D2-инстанса (webworker) после сборки — процесс завершается
- [ ] 3.4 Инвалидация кэша при правке импортируемого `.d2` (проверить свёртку графа)
- [ ] 3.5 Проброс ошибок компиляции D2 понятным сообщением с путём файла

## 4. Шаблон и fixture

- [ ] 4.1 Добавить в `template/src` пример `.d2`-диаграммы + общий `c4lib.d2`
      (аналог `styles.iuml`), с C4-классами и кириллицей
- [ ] 4.2 Убедиться, что `.d2`/`c4lib.d2` попадают в git и в пакет, а выход — в dist

## 5. Golden и CI

- [ ] 5.1 Расширить golden-fixture D2-диаграммой; нормализация покрывает D2-SVG
- [ ] 5.2 Регенерировать эталон (`test:golden:update`), закоммитить; проверить
      детерминизм двух прогонов D2
- [ ] 5.3 Прогнать CI: сборка с `.d2` проходит, процесс не виснет, эталон совпадает

## 6. Документация

- [ ] 6.1 README: как добавить `.d2`-диаграмму, установка опционального движка,
      импорты и общий `c4lib.d2`, выбор `d2Layout`
