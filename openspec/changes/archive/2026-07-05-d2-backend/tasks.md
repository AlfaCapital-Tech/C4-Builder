# Tasks: d2-backend

## 1. Зависимость D2

- [x] 1.1 Добавить `@terrastruct/d2` в `optionalDependencies`; зафиксировать версию
- [x] 1.2 Ленивый `require` движка только при наличии `.d2` в дереве; при отсутствии
      пакета — понятная ошибка с подсказкой установки

## 2. Абстракция рендереров по расширению

- [x] 2.1 Обобщить сбор дерева: вместо `item.pumlFiles` — `item.diagrams[]`
      `{ file, ext, content, engine }`, `engine` = `plantuml` | `d2`
- [x] 2.2 Диспетчеризовать `generateImages` по `engine`; сохранить путь PlantUML
      (java-direct + Smetana) без изменений, включая ditaa
- [x] 2.3 Расширить regex поиска диаграмм в markdown до `\.(puml|d2)`; ссылки/имена
      выходов работают для обоих

## 3. D2-рендерер

- [x] 3.1 Парсер графа импортов `.d2` (`@`/`...@`): сбор `{ путь: контент }` для
      виртуальной `fs` + материал для чексуммы кэша (аналог `foldIncludes`)
- [x] 3.2 Рендер через `compile({ fs, inputPath, options })` → `render` → SVG;
      единый инстанс на прогон (warm-up один раз), конфиг `d2Layout` (dagre|elk)
- [x] 3.3 Явный teardown D2-инстанса (webworker) после сборки — процесс завершается
- [x] 3.4 Инвалидация кэша при правке импортируемого `.d2` (проверить свёртку графа)
- [x] 3.5 Проброс ошибок компиляции D2 понятным сообщением с путём файла

## 4. Шаблон и fixture

- [x] 4.1 Добавить в `template/src` пример `.d2`-диаграммы + общий `c4lib.d2`
      (аналог `styles.iuml`), с C4-классами и кириллицей
- [x] 4.2 Убедиться, что `.d2`/`c4lib.d2` попадают в git и в пакет, а выход — в dist

## 5. Golden и CI

- [x] 5.1 Расширить golden-fixture D2-диаграммой; нормализация покрывает D2-SVG
- [x] 5.2 Регенерировать эталон (`test:golden:update`), закоммитить; проверить
      детерминизм двух прогонов D2
- [x] 5.3 Прогнать CI: сборка с `.d2` проходит, процесс не виснет, эталон совпадает
      (локально подтверждено; в CI класс-диаграмма совпадёт на Temurin 21)

## 6. Документация

- [x] 6.1 README: как добавить `.d2`-диаграмму, установка опционального движка,
      импорты и общий `c4lib.d2`, выбор `d2Layout`
