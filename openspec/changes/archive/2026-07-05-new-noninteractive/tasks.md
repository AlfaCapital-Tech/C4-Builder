# Tasks: new-noninteractive

> Зависимости: делать после `remove-pdf` (полный конфиг без PDF-ключей) и после
> `remove-plantuml-version` (в `new` уже нет выбора версии — флаг `--plantuml-version` не вводим).

## 1. Общий defaultConfig

- [x] 1.1 Вынести значения по умолчанию проекта в единый модуль/объект `defaultConfig`
      (выходы: website + md, `diagramFormat=svg`, локальный рендер, `UTF-8`, под-опции)
- [x] 1.2 Переключить дефолты wizard'а (`cli.collect.js`) на `defaultConfig`
      (значения `default` промптов берутся оттуда) — без изменения интерактивного поведения

## 2. Флаги CLI (cli.js)

- [x] 2.1 Добавить опции `--name <name>`, `--vscode` / `--no-vscode`, `-y, --yes`
- [x] 2.2 Пробросить `opts` в `cmdNewProject(opts)`

## 3. Условные промпты и валидация (cli.new.js)

- [x] 3.1 Сделать промпты (имя, VSCode) условными: значение из флага → без вопроса
- [x] 3.2 Вынести валидацию имени (слэши, непустая папка) и версии в переиспользуемую
      функцию: интерактив — ре-промпт (как сейчас), `--yes` — ошибка + exit≠0
- [x] 3.3 В `--yes` при отсутствии `--name` — понятная ошибка + exit≠0, без промптов
- [x] 3.4 Удалить debug `console.log(isVSCode)` (строка 161)

## 4. Запись полного конфига в --yes

- [x] 4.1 В `--yes` записывать полный `.c4builder` из `defaultConfig` + значений флагов
      (projectName, plantumlVersion), достаточный для сборки без wizard'а
- [x] 4.2 Интерактивный путь оставить как есть (пишет только projectName+plantumlVersion)
- [x] 4.3 Согласовать `generateLocalImages`/рендер в `defaultConfig` (не зависеть от
      ветки «только пинованная версия»)

## 5. Верификация

- [x] 5.1 `c4builder --new --yes --name demo` в пустой папке → без промптов, exit 0
- [x] 5.2 Далее `c4builder` в созданном проекте → сборка без интерактивных вопросов
- [x] 5.3 `--new --yes` без `--name` и с занятой папкой → понятная ошибка, exit≠0, без промптов
- [x] 5.4 Частичный пре-филл без `--yes` (`--name demo`) → спрашивает только версию/VSCode
- [x] 5.5 Дефолты `new --yes` совпадают с дефолтами wizard'а (единый источник)
- [x] 5.6 `openspec validate new-noninteractive --strict` проходит
