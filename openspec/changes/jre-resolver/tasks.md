# Tasks: jre-resolver

> Предусловие: выполняется ТОЛЬКО после `plantuml-java-direct` (там появилась точка
> вызова java). Независимо от `d2-backend`/`resvg-png`/`remove-pdf`.

## 1. Зависимости

- [ ] 1.1 Добавить `tar` и `yauzl` в `dependencies`; зафиксировать (пин) версии

## 2. Модуль резолвера (`jre.js`)

- [ ] 2.1 `resolveJava()` — приоритетная цепочка: системная → кеш → скачивание;
      возвращает абсолютный путь к бинарю `java`; первый годный источник выигрывает
- [ ] 2.2 Детект системной: `JAVA_HOME/bin/java`, затем `PATH`; `java -version` → парс
      мажорной версии; годна при мажоре ≥ 17; отсутствие/ошибка запуска — переход к
      следующему источнику (не падение)
- [ ] 2.3 Кеш: путь `~/.cache/c4builder/jre/temurin-21-<os>-<arch>/` (Windows —
      `%LOCALAPPDATA%`); валидность = наличие `bin/java`(`.exe`); битую установку
      перекачивать
- [ ] 2.4 Загрузчик Adoptium: assets API (`/v3/assets/latest/21/hotspot?...image_type=jre&vendor=eclipse`)
      → `{ link, sha256 }`; скачать по link; сверить sha256 ДО распаковки; несовпадение
      → ошибка, кеш не заполнять
- [ ] 2.5 Маппинг `process.platform`/`process.arch` → `os`/`arch` Adoptium; выбор
      формата архива (`tar.gz` linux/mac → `tar`, `zip` windows → `yauzl`); распаковка
      в кеш
- [ ] 2.6 Понятная ошибка при полном провале цепочки (поставить JRE 17+ либо
      `c4builder jre install`), без сырого стектрейса spawn

## 3. Встраивание в `build.js`

- [ ] 3.1 В `generateImages` резолвить `javaBin` один раз ПЕРЕД циклом, только если в
      дереве есть хотя бы одна `engine === 'plantuml'` диаграмма
- [ ] 3.2 `renderDiagram` принимает `javaBin` в опциях; `spawn(javaBin, argv)` вместо
      `spawn('java', argv)`
- [ ] 3.3 Проект без `.puml` (только `.d2`) → `resolveJava` не вызывается, скачивание
      не инициируется

## 4. CLI — прогрев

- [ ] 4.1 Подкоманда `c4builder jre install`: резолвит/скачивает JRE в кеш заранее, без
      полной сборки; печатает путь и версию
- [ ] 4.2 (open) Поведение при годной системной java: сообщать «скачивание не требуется»
      против флага `--force` (см. design Open Questions)

## 5. Верификация

- [ ] 5.1 Системная java 17+ есть → используется, скачивание не выполняется, сеть не
      задействуется
- [ ] 5.2 Системной java нет → скачивается Temurin 21 JRE под текущую платформу, sha256
      совпадает, PlantUML-диаграммы рендерятся
- [ ] 5.3 Второй прогон → java берётся из кеша, без сети
- [ ] 5.4 Проект только из `.d2` → java не резолвится, скачивания нет
- [ ] 5.5 Офлайн, нет системной java и пустой кеш → понятная ошибка с двумя действиями,
      D2-рендер (если есть) при этом работает
- [ ] 5.6 `c4builder jre install` → JRE в кеше, последующая сборка не качает
- [ ] 5.7 Golden CI (Temurin 21 через `setup-java`) зелёный: срабатывает ветка
      «системная годная», layout не меняется
- [ ] 5.8 `openspec validate jre-resolver --strict` проходит
