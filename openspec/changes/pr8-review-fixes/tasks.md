<!-- САМОДОСТАТОЧНЫЙ КОНТЕКСТ (для работы после сброса контекста):
Ветка refactor/phoenix, PR #8 (draft снят, НЕ мержить до фиксов). Все находки —
из max-ревью 2026-07-17: 10 finder-углов, каждый кандидат верифицирован отдельным
агентом (вердикты в тексте задач), sweep-проход. Номера строк — на момент ревью
(коммит be99acd), при дрейфе искать по описанию механики.
Сборка: npm run build (tsc → dist/). Тесты: npm test; golden: npm run test:golden,
перегенерация эталонов: npm run test:golden:update (нужна после фиксов секции 4!).
Правило репо: коммиты БЕЗ номера задачи (публичный ai-sandbox), стиль — как
`rc-publish: ...` в git log. После каждой секции — сборка + тесты. -->

## 1. Регрессии конфига (schema.ts / dispatch.ts / collect.ts)

- [x] 1.1 `schema.ts:32,33` (CONFIRMED, рантайм): `z.coerce.string()` превращает boolean в `'false'/'true'` — `docsifyTemplate:false` → `require('<cwd>/false')` → MODULE_NOT_FOUND; `webPort:false` → `app.listen('false')` слушает unix-сокет. Фикс: в `nullish`-препроцессе (или отдельным preprocess для строковых полей) трактовать `false` как «не задано» (→ дефолт), либо запретить boolean понятной ошибкой; для `webPort` дополнительно валидировать что строка — число 1..65535 (закроет и опечатку «30OO», которая сейчас тоже даёт unix-сокет + битый URL «localhost:30OO»; parseInt-флаг `-p 30OO` тихо биндит порт 30 — заодно `parseInt` → строгая проверка). Проверка: unit на safeParse с false/числом + ручной `--site`.
- [x] 1.2 `schema.ts:48` (CONFIRMED, рантайм): `null` внутри `excludeSidebarFolderByPath` валит строгую валидацию с exit 1 (master молча игнорировал; runtime-guard в `markdown.ts:330` теперь мёртв — заодно упростить до `?.some(startsWith)`). Фикс: препроцесс массива — отфильтровать не-строки (можно с warn), z.array(z.string()) после фильтра. Проверка: unit safeParse `['a', null]` → ok.
- [x] 1.3 `dispatch.ts:215` + `collect.ts:96-230` (CONFIRMED): ловушка salvage — визард не переспрашивает вложенные битые ключи (блоки гейтятся по валидным generate*-флагам; diagramsOnTop вообще не входит в условие своего блока), `conf.set('hasRun',true)` (dispatch.ts:207) пишется ДО строгого `parseConfig` (214) → exit 1 навсегда (следующие запуски падают на 201 до визарда). Фикс (минимальный, без перестройки визарда): на salvage-пути перед вызовом визарда физически перезаписать битые ключи salvaged-значениями (issues уже известны — `conf.set(key, salvaged[key])` по списку), и/или писать hasRun только ПОСЛЕ успешного parseConfig. Проверка: e2e-сценарий из ревью — `.c4builder` с `"includeNavigation":"yes"` без hasRun → запуск → повторный запуск должен работать.
- [x] 1.4 `dispatch.ts:51-52` (CONFIRMED): `k in raw` считает `"rootFolder": null` заданным (nullish уже превратил в дефолт 'src') → визард молча пропускает промпт, вопреки комментарию схемы «null = не задан». Фикс: фильтровать ключи со значением null так же, как отсутствующие (`k in raw && raw[k] !== null`). Проверка: конфиг с rootFolder:null → визард спрашивает папку.

## 2. Lifecycle сборки (build.ts / diagrams.ts)

- [x] 2.1 `build.ts:97-104` (CONFIRMED): finally безусловно сносит dist_bk — при упавшей сборке уничтожается единственная полная копия прошлого билда (master удалял только после всех фаз). Фикс: удалять dist_bk только при успехе (флаг ok после фаз; в finally — только teardownD2), при падении оставить dist_bk и написать в лог, что бэкап сохранён. Проверка: искусственно уронить рендер (битый .puml) → dist_bk на месте.
- [x] 2.2 `diagrams.ts:396-400` (CONFIRMED, waste): `throw failed.reason` раньше `cacheConf.set('checksums', newChecksums)` — чексуммы успешно отрендеренных в упавшем прогоне теряются. Фикс: set до throw (или в finally генерации), сохранять чексуммы только реально отрендеренных/восстановленных. Вместе с 2.1 повторный прогон после сбоя чинит только упавшую диаграмму.

## 3. JRE (jre.ts / diagrams.ts)

- [x] 3.1 `diagrams.ts:269-273` (CONFIRMED, регрессия): needsJava по наличию PlantUML-диаграмм → resolveJava (вплоть до скачивания ~50 МБ) даже когда всё восстановится из кеша; офлайн-машина без java с собранным проектом теперь падает. Фикс: ленивый резолв — резолвить при ПЕРВОЙ диаграмме, реально требующей рендера (промис-синглтон внутри generateImages после решения «кеш не сработал»). Спека jre-resolution фиксирует «ленивый резолв» — фикс возвращает соответствие. Проверка: собранный проект + PATH без java → повторная сборка проходит.
- [x] 3.2 `jre.ts:150-161,387-388` (CONFIRMED): cachedJava не проверяет запускаемость (detectSystemJava гоняет javaMajor, кеш — нет) → полубитый кеш валит сборку без фолбэка. Фикс: прогонять javaMajor и на кешированном бинаре; при провале — сносить каталог кеша и идти в download.
- [x] 3.3 `jre.ts:326,345-346` (CONFIRMED): установка в общий кеш rmSync→renameSync без межпроцессной блокировки: дубль-скачивание на холодном кеше, окно ENOENT, ENOTEMPTY при гонке. Фикс: если dir уже валиден к моменту установки — просто выбросить свой stage (перепроверка после скачивания); rename без предварительного rm (rename поверх существующего → catch EEXIST/ENOTEMPTY = «другой процесс успел» → ок); опционально lockfile.
- [x] 3.4 `jre.ts:281` (CONFIRMED, security): zip-slip в extractZip — `path.join(destDir, entry.fileName)` без containment-проверки (tar-ветка защищена node-tar≥6). Фикс: `const dest = path.resolve(destDir, entry.fileName); if (!dest.startsWith(path.resolve(destDir) + path.sep)) throw` (+ пропуск симлинков). Проверка: unit с фейковой записью `../evil`.

## 4. Compose (markdown.ts / tree.ts) — МЕНЯЕТ GOLDEN

- [x] 4.1 `markdown.ts:122` (CONFIRMED, унаследовано из master, зафиксировано в golden!): в complete-md `![diagram](diagramUrl)` без префикса папки (linkHref на 106-109 префикс имеет) → битые картинки для диаграмм вне корня. Фикс: в complete-варианте не-remote ветки использовать префиксованный путь и для image (тот же расчёт, что linkHref). После фикса: `npm run test:golden:update` + глазами проверить `test/golden/*/tree/*-fixture.md` — пути должны стать `<папка>/<имя>.svg`.
- [x] 4.2 `markdown.ts:43-48,167` (CONFIRMED): при пользовательском h1 + diagramsOnTop нессылочные диаграммы попадают ВЫШЕ h1 (injectAfterFirstH1 заглатывает блок диаграмм вместе с заголовком). Фикс: структурная сборка — выделять пользовательский h1 из texts[0] ДО compileDocument (или передавать ownH1 внутрь и вставлять diagrams после заголовка), порядок всегда h1 → chrome → (диаграммы|текст по DIAGRAMS_ON_TOP). Добавить тест-кейс: md с `# Title` + нессылочная диаграмма + diagramsOnTop (golden не покрывает — ни один template/src/*.md не начинается с '# ').
- [x] 4.3 `tree.ts:84-91` + guard :60 (CONFIRMED): DIST_FOLDER попадает в descendants до раннего return рекурсии → битая ссылка в nav-меню + мусорный docs/docs при rootFolder='.'. Фикс: проверять `path.join(dir, file) === options.DIST_FOLDER` ДО push в descendants/makeDirectory (guard внутри цикла, а не только в рекурсии).
- [x] 4.4 `markdown.ts:372,387` (CONFIRMED, рантайм): require ESM-шаблона возвращает namespace → «docsifyTemplate is not a function». Фикс: `const m = require(...); docsifyTemplate = typeof m === 'function' ? m : (typeof m?.default === 'function' ? m.default : throw понятная ошибка)`.

## 5. Рендер-инфраструктура (d2renderer.ts / diagrams.ts)

- [x] 5.1 `d2renderer.ts:64-68` (CONFIRMED): teardownD2 в finally build() повторно await-ит упавший inst.ready и затирает исходную ошибку сборки. Фикс: `try { await inst.ready } catch { /* уже отражено ошибкой рендера */ }` + terminate в finally; teardown идемпотентен.
- [x] 5.2 `diagrams.ts:107-128` (CONFIRMED): второй httpGet без таймаутов и редиректов (двойник jre.ts:175-204 имеет оба) — embed-сборка с онлайн-рендером виснет навсегда/падает на 302. Фикс: общий хелпер (util/http.ts): jre-версия (redirect-follow + CONNECT/IDLE таймауты) + обёртка «собрать тело в Buffer»; заодно вычистить стену тривиальных легаси-комментариев (7 шт., нарушают правило «комментарии только важные»).

## 6. Ниже капа — опционально, по желанию (НЕ блокирует мерж PR #8)

- [x] 6.1 `dispatch.ts:151` (PLAUSIBLE): Configstore для .c4builder.cache — RMW-гонка конкурентных запусков (watch + ручная сборка). Следствие — потеря чексумм/лишний перерендер, не порча. Вариант: пропускать запись кеша в watch-режиме или file-lock. → Сделано file-lock'ом на всю сборку (`src/util/lock.ts` + обвязка в dispatch): чинит заодно и гонку dist/dist_bk; протухший лок (мёртвый pid / старше часа) снимается сам; watch пропускает ребилд с коротким сообщением; юнит-тесты `test/lock.test.mjs` + ручной E2E.
- [x] 6.2 `ci.yml:83-97` (CONFIRMED): grep констант TEMURIN_FEATURE/JRE_CACHE_SCHEMA из jre.ts без гейта на пустой результат (echo глотает код grep даже под bash -e) + хардкод пути `~/.cache/c4builder/jre` (XDG_CACHE_HOME разъедется). Фикс: `[ -n "$v" ] || exit 1` после извлечения; лучше — `c4builder jre info --json` (CLI-подкоманда, отдающая путь и материал ключа). → Сделана подкоманда `c4builder jre info` (всегда JSON: feature/schema/cacheDir); CI берёт параметры из неё через jq с гейтом на пустые значения, путь кеша — из cacheDir.
- [x] 6.3 `d2renderer.ts:190` + `tree.ts:189` (механика подтверждена чтением): чексуммы включают АБСОЛЮТНЫЕ пути импортов (.d2 и .iuml) — кеш непереносим между машинами/чекаутами. Фикс: класть в материал путь относительно ROOT_FOLDER. → Сделано: путь относительно cwd, posix-разделители (переносимо и между ОС); существующие кеши инвалидируются один раз (штатный перерендер).
- [x] 6.4 `d2renderer.ts:98` (подтверждено чтением): D2-файлы читаются hardcoded utf-8, опция CHARSET игнорируется (PlantUML честно получает -charset). Фикс: читать с CHARSET или задокументировать «D2 = только UTF-8». → Решение: D2 = только UTF-8 (язык определён поверх UTF-8; iconv-зависимость не оправдана), задокументировано у места чтения.
- [x] 6.5 `d2renderer.ts:133-142` (подтверждено чтением): commonAncestor на пустом наборе файлов (вход не прочитался — гонка в watch) → `Math.min()`=Infinity → сырой TypeError вместо понятной ошибки. Фикс: guard на пустой files с внятным сообщением. → Сделано в buildCompileRequest.
- [x] 6.6 Перф-темы (CONFIRMED, по желанию): JVM-спавн на каждую диаграмму (батчинг осложнён выбором -pipe ради контроля имён — см. коммент diagrams.ts:130-133); синхронный resvg.render → renderAsync; foldIncludes без мемоизации (N×M чтений .iuml — у D2 filesMemo уже есть); httpGet embed-ветка мимо diagramBase64Cache; teardownD2/пересоздание воркера на каждый watch-ребилд; statSync×2 в tree.ts (withFileTypes); golden на обеих ногах CI-матрицы; docker-publish без buildx-кеша; base64-кеш не чистится в finally (память watch). → Сделано всё, КРОМЕ JVM-батчинга (осознанно: -pipe нужен для контроля имён выхода, пул уже параллелит; фиксируем как принятое решение): renderAsync; кеш чтения include (clearIncludeCache); embed-httpGet через diagramBase64Cache; teardownD2 перенесён в dispatch (в watch воркер живёт между ребилдами, кеш графа сбрасывает clearD2FileCache); withFileTypes (симлинкам — statSync); golden на одной ноге матрицы (node 24), вторая — `npm run test:unit`; buildx gha-кеш в docker-publish; все пофайловые кеши чистятся и в finally build().
- [x] 6.7 Cleanup-темы (перечислены в ревью, без вердиктов): дубли relative-link блоков ×3 и якорей ×2 в markdown.ts; splice(1) сайдбара vs replace(ROOT_FOLDER) (ломается на многосегментном rootFolder); тернарники ×9 и find→includes ×11 в collect.ts; generateTemplate=40 строк вместо fsextra.copy; IO-обёртки utils.ts (мёртвый параметр type, глотание EACCES в makeDirectory); мёртвые экспорты tree.ts (DiagramExt и др., ложный коммент «для тестов» у diagramRefRegex); 24×nullish(...) в schema.ts; generateWebMD дублирует скелет generateMD (trimStart уже разъехался); движковые if/else мимо реестра DIAGRAM_ENGINES (~9 веток в 4 файлах); тесты импортируют dist/ глубокими путями (уронит будущий build-split); утечка temp-папок в golden.test.mjs beforeAll при падении посреди цикла. → Сделано: relativeMdLink/mdAnchor/compilePage в markdown.ts (golden байт-в-байт); сайдбар через path.relative (заодно чинит ссылки при rootFolder='.'); checkedKey+includes в collect.ts; generateTemplate = fsextra.copy; utils.ts на fs.promises (mkdir -p, EACCES пробрасывается, Buffer-возврат readFile); DiagramExt удалён, diagramRefRegex деэкспортирован; 24×nullish закрыт ещё секцией 1; реестр расширен способностью remoteRender (build/compose ветвятся по ней; render-ветки PlantUML/D2 остаются осознанно — пути реально разные); тесты ходят в dist/ через единый test/dist.mjs; фикстуры golden регистрируются до сборки + anyFailed при падении хука.
